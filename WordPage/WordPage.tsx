import {Text, View} from 'react-native';

const WordPage = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>내 단어장 목록 페이지</Text>
    </View>
  );
};

export default WordPage;
